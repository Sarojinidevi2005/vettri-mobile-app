import 'package:flutter/material.dart';

class Move extends StatelessWidget {
  const Move({super.key, required String ss});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("data"),
      ),
    );
  }
}